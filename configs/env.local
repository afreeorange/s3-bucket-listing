# Some arbitrary bucket for local development
BUCKET="public.nikhil.io"

# NEED TRAILING SLASH IF SPECIFIED!
PREFIX=""

# Use this to build the final artifacts via --public-url
PUBLIC_URL="/"

# Go into HTML metadata
TITLE="Nikhil's Public Files"
DESCRIPTION="Because what could go wrong with a public dirlisting?"
